<!-- file to define the layout of a row that in turn will be output to a delimited or fixed                            -->
<!-- length ASCII file                                                                                                 -->

<!-- row type may be [fixed] or [delimited]. in case of [delimited] you need to specify an attribute:                  -->
<!-- [seperator]. e.g. seperator=";", to indicate the type of seperator to be used between the fields                  -->

<!-- reference fields can be defined which will not be sent to the output file. they may be referenced by other fields.-->
<!-- this is helpful especially with datetime fields, as the reference to another datetime field references its date   -->
<!-- and time value. this means that datetime values across multiple columns "make sense", meaning are real dates.     -->

<!-- example reference fields:                                                                                         -->
<!--		<field type="datetime" id="date1" pattern="yyyy-MM-dd"/>                                                   -->
<!--		<field type="datetime" id="year" reference="date1" pattern="yyyy"/>                                        -->

<!-- the example shows a reference field named "date1" and another named "year" referencing the first one. the fields  -->
<!-- will be based on the same date but use a different pattern as displayed.                                          -->

<!-- fields have a type attribute: [category], [random] or [regex]. If you specify [category] a word from the category -->
<!-- files is used; if you specify [random] then the value is generated randomly and when you specify [regex] then the -->
<!-- pattern of the regular expression needs to be specified using the [pattern] attribute.                            -->

<!-- note that the attribute [category] must correspond to the name of the category file                               -->
<!-- e.g. <field type="category" category="names" length="20" /> references the category file: [names.category]        -->

<!-- the length attribute specifies the maximum length of the field. if the generated value is longer, it will be      -->
<!-- shortened. if it is shorter - in case of a fixed length output file - the value will be filled with               -->
<!-- trailing spaces up to the specified length.                                                                       -->

<!-- you may use a field that is declared as a reference field, by setting the field type to "reference" and           -->
<!-- settings the reference attribute to the id of referenced field. you may use one or multiple reference fields      -->

<!-- e.g.: <field type="reference" reference="airport/_/airline/-test" length="20" /> will be output as HAM_Swiss-test -->

<!-- if a reference field is not found the literal value is used for output. so this allows you to construct fields    -->
<!-- from one or multiple other fields.                                                                                -->

<!-- reference fields do not need a length attribute except from reference fields of type regex and random. each       -->
<!-- reference fields needs to have an attribute of "id" specified.                                                    -->

<!-- reference fields can also use other reference fields. make sure you do create circular references.                -->                                                             -->

<xml> 
	<references>
		<field type="datetime" id="date1" pattern="yyyy-MM-dd"/>
		<field type="datetime" id="year" reference="date1" pattern="yyyy"/>
		<field type="datetime" id="quarter" reference="date1" pattern="Q"/>
		<field type="datetime" id="weekday" reference="date1" pattern="EEEE"/>
		<field type="category" id="airport" category="airport_code"/>
	</references>
	<row type="delimited" seperator=";"> 
		<field type="category" category="seasons" length="20" /> 
		<field type="datetime" pattern="q" length="1" /> 
		<field type="random" length="10" /> 
		<field type="regex" pattern="www.[a-zA-Z]{8,16}.com" length="30" /> 
		<field type="reference" reference="date1" length="10" />
		<field type="reference" reference="year/-/quarter" length="20" />
		<field type="reference" reference="weekday" length="10" />	
		<field type="reference" reference="code:/airport/_test" length="20"/>	
	</row> 
</xml>

